apiVersion: v1
kind: Service
metadata:
  name: my-webapp
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: my-webapp
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
